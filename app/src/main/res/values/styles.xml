<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="AppTheme.Base">
        <!-- Customize your theme here. -->
    </style>

    <style name="AppTheme.Base" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="colorPrimary">@color/primaryColour</item>
        <item name="colorPrimaryDark">@color/primaryColourDark</item>
        <item name="colorAccent">@color/accentColour</item>
        <item name="colorControlHighlight">@color/highlightColour</item>
        <item name="colorButtonNormal">@color/md_indigo_500</item>
        <item name="android:textColorPrimary">@color/textColourPrimary</item>
        <item name="android:textColorSecondary">@color/textColourSecondary</item>
    </style>

    <style name="CardView.Button">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginBottom">4dp</item>
        <item name="android:clickable">true</item>
        <item name="android:foreground">?android:attr/selectableItemBackground</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>